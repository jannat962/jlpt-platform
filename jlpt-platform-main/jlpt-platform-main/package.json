{
  "name": "jlpt-platform-root",
  "version": "1.0.0",
  "description": "JLPT Mock Test Platform",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && .\\venv\\Scripts\\python.exe -m uvicorn app.main:app --reload\"",
    "install-all": "npm install --prefix frontend && cd backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
